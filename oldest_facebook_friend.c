#include<stdio.h>
struct friends
{
    char name;
    int day;
    int month;
    int year;
    int total;
};

int main()
{
    struct friends old[3];
    int i;
    int p_d,p_m,p_y;
    printf("ENTER THE PRESENT DATE: ");
    scanf("%d %d %d",&p_d,&p_m,&p_y);
    for(i=0;i<3;i++)
    {
        scanf("%s%d%d%d",&old[i].name,&old[i].day,&old[i].month,&old[i].year);
        if(old[i].month==1||old[i].month==3||old[i].month==5||old[i].month==7||old[i].month==9||old[i].month==11)
        {
            old[i].total=ol
        }
    }
    for(i=0;i<3;i++)
    {
        old.year[i]
    }


}
